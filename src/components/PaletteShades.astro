---
const shades = ['', 50, 100, 200, 300, 400, 500, 600, 700, 800, 900, 950]
---

{
  shades.map((shade) => (
    <div class="hidden xs:block pb-1 sm:pb-2" aria-hidden="true">
      {shade}
    </div>
  ))
}
